# RailLOOP
Static Railway Logging web app feat. React, built with Vite.

## Pre-submit
Run the following command before commit:

```bash
npm run pre-submit
```

`pre-submit` includes:
- release content consistency check
- main app build
- blog tests
- blog build
