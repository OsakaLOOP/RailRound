---
import RedirectPage from '../components/RedirectPage.astro';
import { DEFAULT_BLOG_LANG } from '../utils/blogRouting';

const to = `/blog/${DEFAULT_BLOG_LANG}/`;
---

<RedirectPage to={to} message="Redirecting to default blog language..." />
